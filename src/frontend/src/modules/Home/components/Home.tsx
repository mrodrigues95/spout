import { PrimaryLayout } from '~/shared/components';

const Home = () => {
  return (
    <PrimaryLayout title="Home">
      <h1>HOME</h1>
    </PrimaryLayout>
  );
};

export default Home;

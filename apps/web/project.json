{
  "root": "apps/web",
  "sourceRoot": "apps/web/src",
  "projectType": "application",
  "generators": {},
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "root": "apps/web",
        "outputPath": "dist/apps/web"
      },
      "configurations": {
        "production": {}
      }
    },
    "dev": {
      "executor": "@nrwl/next:server",
      "options": {
        "buildTarget": "web:build",
        "dev": true,
        "port": 3000
      },
      "configurations": {
        "production": {
          "buildTarget": "web:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "web:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/web"],
      "options": {
        "jestConfig": "apps/web/jest.config.js",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "options": {
        "lintFilePatterns": ["apps/web/**/*.{ts,tsx,js,jsx}"]
      },
      "outputs": ["{options.outputFile}"]
    },
    "compile": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "commands": [
          "yarn run relay-compiler --src ./apps/web/src --schema ./apps/web/schema.graphql --exclude ['**/node_modules/**', '**/__generated__/**'] --extensions ['ts', 'tsx'] --language typescript"
        ]
      }
    },
    "get-schema": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "commands": [
          "get-graphql-schema http://localhost:5000/api/graphql > schema.graphql"
        ],
        "cwd": "apps/web"
      }
    },
    "codegen-build": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "commands": ["npx graphql-codegen"],
        "cwd": "apps/web"
      }
    },
    "codegen-watch": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "commands": ["npx graphql-codegen --watch"],
        "cwd": "apps/web"
      }
    },
    "codegen-clean": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "commands": ["trash \"./src/**/__generated__/**\""],
        "cwd": "apps/web"
      }
    }
  }
}
